# Task Result: claude

Subagent 'claude' completed in 121.8s

---

Both `HashMap` and `LinkedHashMap` are covered by the existing `import java.util.*` at line 54. Good.

The test `testExactDecodeLoopMemoryIsolation` is now at the end of `TestDspValidation.java`. Here's what it does:

**Warmup (3 steps):** Runs with completely fixed/identical inputs to establish baseline — should show ~0 MB/step growth since nothing changes.

**5 Variants (5 steps each), each changing ONE thing:**

| Variant | What changes | Tests |
|---------|-------------|-------|
| **A** `NEW_HASHMAP` | New `HashMap` wrapper each step, same array references | Whether HashMap allocation triggers internal bookkeeping |
| **B** `CLEAR_PLACEHOLDERS` | `clearPlaceholders(false)` before each step | Whether placeholder clearing causes re-association overhead |
| **C** `ASSOCIATE_POSITION_IDS` | `associateArrayWithVariable` for position_ids | Whether explicit variable association leaks |
| **D** `GROWING_ATTN_MASK` | New attention_mask with growing shape each step | Whether shape changes trigger the 256MB allocation |
| **E** `FULL_BUILD_INPUT_MAP` | Full `DecoderUtils.buildDecoderInputMap` path | The complete decode loop path as control |

Each variant logs `[VARIANT] name=X step=N gpuFree=YMB delta=ZMB` and a total summary. Whichever variant shows ~256MB/step delta is the culprit.
