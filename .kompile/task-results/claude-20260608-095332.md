# Task Result: claude

Subagent 'claude' completed in 370.0s

---

Now I have a comprehensive picture. Let me compile the findings.

---

## Findings: May 2026 SmolDocling/VLM Benchmark Activity

### Summary

**Kompile test milestones**: Zero May 2026 milestones exist. All milestones in the store are from **June 6, 2026** (batch58–batch105). One VLM-related milestone exists (`batch87_dsp_vlm_stalled_jun6`) but it's a CPU-backend skip marker, not an actual benchmark run.

**Kompile transcripts**: Zero May 2026 benchmark run transcripts. The 63 saved conversations contain no `run-benchmark.sh` invocations, no `SmolDocling` mentions, and no `lateSteady`/`tok/s` output. One session (`passthrough-5e73c57d`, 2026-05-31) references tok/s in a code comment and VLM device routing, but this is a kompile/anserini session, not a DL4J benchmark run.

**Kompile tool-call catalog**: Shows run-benchmark.sh Bash calls with two timestamp clusters:
- **2026-05-14** — process checks (`ps aux | grep run-benchmark`), benchmark invocations (`./run-benchmark.sh --tokens 250`), and TestSmolDocling test runs. These are **actual benchmark runs** but the timestamps appear to be catalog-indexed dates (all within the same second), not necessarily the original execution dates.
- **2026-06-03** — full DSP benchmark sweep (`SLOT_BY_SLOT`, `OPTIMAL` with `--tokens 250`, `--op-timing`, `--diag-replay`).

**Benchmark log files on disk**: All `/tmp/*bench*.log` and `/tmp/vlm-bench-*.log` files have filesystem dates of **June 7-8, 2026**. The `dl4j-may18-benchmark.log` and `worktree-may18-benchmark.log` are from **June 8** (worktree regression checks against a May 18 commit, not May-era runs).

### Actual May 2026 Benchmark Evidence (from git history)

Git commits are the only reliable record. Each `perf:` commit implies a benchmark run because the commit messages cite tok/s results:

| Date | Commit | tok/s | What was benchmarked |
|------|--------|-------|---------------------|
| May 3 | 56f98a279c | — | Isolation tests (GQA, RMS, gated delta, optimizer) |
| May 4 | 01880220b4 | ~61 | Multi-consumer in-place protection |
| May 6 | 7730623ea9 | 12 | BROKEN: performance regression |
| May 8 | ce6e6ecb6a | 12 | Correctness baseline snapshot |
| May 16 | f74e8a4eca | 32.2 | Correctness snapshot, all DSP tests pass |
| May 17 | df2ffab4b2 | 41.79 | Gap capture relaxation |
| May 17 | 37bb6c60ff | 37 | REGRESSION from FlashAttention changes |
| May 18 | 3f5609d9db | 43 | Revert FlashAttention regression |
| May 18 | c1cef1548f | 49.76 | Batched GEMM mixed-type cast (+21%) |
| May 18 | 237ee85729 | 50.19 | Persistent bgemm cast scratch |
| May 18 | 9ae7f83d4c | 58.60 | cuBLAS workspace hoisting |
| May 19 | 5ff8b03a72 | 67.35 | W1 late-steady metric |
| May 19 | 9bb73c1a7e | 69.24 | Skip helper dispatch for gap-fast ops |
| May 19 | 040372a896 | 69.57 | cuBLAS Lt for gap matmul |
| May 28 | b36bb32e7b | 66.71 | N107 skip cuBLAS setup in MmulHelper |
| May 28 | f0b362e8e6 | — | DSP replay + VLM regression fixes |

### Key Distinction

- **Actual benchmark runs**: Evidenced only by git commit messages citing tok/s. No benchmark logs, milestone records, or transcript captures survive from May.
- **Memory milestones**: The `perf-optimization-todo.md` and `monolithic-vs-composite-replay-regression.md` memory files contain detailed May benchmark results (50-69 tok/s range, step-time breakdowns, dead-end experiments), but these are **reconstructed summaries** written into memory, not live milestone records.
- **Kompile test milestones**: All date from June 6 — none from May. The milestone system appears to have been initialized during the June test sweep.
- **Disk logs**: All overwritten by June 7-8 runs. No May logs survive.
