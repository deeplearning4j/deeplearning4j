# Task Result: qwen

Subagent 'qwen' completed in 64.5s

---

Done. Here's what I found and fixed:

**`ShapeUtils::evalPermutedViewShapeInfo` already handles rank mismatch** (the `rank > permLen` case with leading size-1 dims). The delegation from `buildPermutedViewShapeInfo` was structurally fine — no rank-mismatch regression existed in the current code.

**What was broken: negative permutation indices.** Neither `buildPermutedViewShapeInfo` nor `ShapeUtils::evalPermutedViewShapeInfo` handled negative dimension indices (e.g., `-1` meaning the last dimension). The `ShapeUtils` check `if (srcDim < 0 || srcDim >= rank)` would reject them outright.

**Fixes applied:**

1. **`NativeDynamicShapePlan_slotexec.cpp`** — `buildPermutedViewShapeInfo` now normalizes negative indices before delegating: `if (dim < 0) dim += rank`.

2. **`ShapeUtils.cpp`** — `evalPermutedViewShapeInfo` now also normalizes negative indices in both the `permLen >= rank` and rank-mismatch adaptation paths, making it robust for all callers.