# Task Result: claude

Subagent 'claude' completed in 145.0s

---

Both edits look correct. Here's what was changed:

**Bug 1 — `ModelIOConfig.discover()` (`ModelIOConfig.java:310-328`)**:

Added a scan of `model.externalInputs()` before the existing `hasVariable("input_ids")` fallback. `model.inputs()` only returns PLACEHOLDER-typed variables, but ONNX import may classify `input_ids` as a VARIABLE or ARRAY type. `externalInputs()` returns all graph inputs regardless of type (PLACEHOLDER, VARIABLE, ARRAY, CONSTANT). The new code iterates `externalInputs()` looking for any name containing `"input_id"`, matching the same pattern used in the main loop.

**Bug 2 — `DecoderUtils.buildDecoderInputMap()` (`DecoderUtils.java:864-881`)**:

Added an `else` branch for the non-padded case. Previously, the `attn_mask_reformat` override was only provided when `usePadded=true` (DSP active + static KV). During non-padded prefill (step 0, `dspActive=false`), the override was skipped entirely, letting the model's internal `attn_mask_reformat` subgraph run — which produces wrong results for VLM. The new branch builds the same 4D causal bias `[1, 1, currentSeqLen, totalSeqLen]` but uses `totalSeqLen = pastSeqLen + currentSeqLen` (actual KV length) instead of `maxKvLen + currentSeqLen` (padded length). For prefill at step 0, `pastSeqLen=0` so `totalSeqLen=currentSeqLen`, producing a standard lower-triangular causal mask.
