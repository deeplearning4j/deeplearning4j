# Task Result: claude

Subagent 'claude' completed in 177.4s

---

This is the smoking gun. Line 472:

```cpp
isFirstFrozenWarmup = shapesFrozen && executeCount == 0;
```

Here's the complete chain of events:

## Investigation Summary

### The `markExternalInputVariable` Regression Mechanism

**Timeline of what happens:**

1. **Before decode loop** (lines 443-450): `markExternalInputVariable()` is called for 6-8 external inputs

2. **First call** (e.g., `embeddingsExtIdx`): The `needsFullInvalidation` check at line 2654 evaluates based on whether `effectiveExternals_`, `placeholderStagingBuffers_`, `cachedVariableExtIndices_`, or `variableIndicesCached_` are populated. If the plan has already gone through warmup+capture (which it has — Java prefill runs the plan), **these are populated**, so `needsFullInvalidation = true`.

3. **Full invalidation fires** (lines 2665-2687):
   - Deletes `effectiveExternals_` and `placeholderStagingBuffers_`
   - Calls `invalidateForRebuild()` on **every segment**
   - Sets `pointersStable_ = false`

4. **`invalidateForRebuild`** (DspSegmentLifecycle.h:147-178) does:
   - Cleans up captured CUDA graphs (`cleanupSegmentForRebuild`)
   - Resets `argTableStable = false`
   - Sets `executionCount = 0`
   - Sets lifecycle to `NEEDS_WARMUP`
   - **Calls `plan->resetExecuteCount()`** → sets `executeCount_ = 0`
   - **Calls `plan->resetFrozenConstantDetection()`**

5. **Second `markExternalInputVariable` call**: `effectiveExternals_` is now null (deleted), `placeholderStagingBuffers_` is null, `cachedVariableExtIndices_` is now empty. So `needsFullInvalidation = false`. Only the flag `externalInputIsVariable_[idx] = true` is set — no segment invalidation.

6. **But the damage is done.** After the first call that triggered invalidation: `executeCount_ = 0`, all segments are in `NEEDS_WARMUP`, captured CUDA graphs are destroyed.

7. **Decode loop step 0**: `executeSteadyState()` is called. Check at line 2409:
   ```cpp
   executeCount_ < 3  // TRUE (executeCount_ == 0)
   ```
   Falls back to full `execute()`.

8. **Inside `execute()`**: `populateDerivedState` computes:
   ```cpp
   isFirstFrozenWarmup = shapesFrozen && executeCount == 0;  // TRUE
   ```
   So the plan goes through **phaseWarmup** (line 1954-1955).

9. **phaseWarmup re-executes every slot slot-by-slot**, resets all segment state, and the plan starts over from scratch — re-warming up, re-capturing CUDA graphs, re-compiling.

### The Regression

This means the **first ~3 decode steps** after `markExternalInputVariable` are executed via **slot-by-slot warmup**, not CUDA graph replay. This causes:

1. **Performance regression**: 3+ steps of full slot-by-slot execution instead of graph replay
2. **Potential accuracy regression**: phaseWarmup re-runs `detectFrozenConstants()` and `resegmentForFreeze()` (line 2896). If the re-warmup computes different shapes or frozen classifications than the original warmup (because the decode-step shapes differ from prefill shapes), the plan could end up with different segment boundaries or different frozen constant classifications.
3. **Stale state risk**: The `resetFrozenConstantDetection()` call means that constants detected during the original prefill-time warmup are re-detected with decode-step inputs — slots that were frozen constants during prefill may not be frozen constants during decode (different sequence length), leading to different execution paths.

### CPU Version Comparison

The CPU version (`autoregressive_decode.cpp`) does **NOT** have the `markExternalInputVariable` calls at all. It also uses `plan->execute()` (not `executeSteadyState`), and explicitly syncs inputs to device after each step (lines 584-598). The CPU path is simpler because there are no CUDA graphs to invalidate.

### Key Concerns

1. **The `needsFullInvalidation` condition is fragile**: Only the first `markExternalInputVariable` call triggers full invalidation. The remaining 5-7 calls just set flags. But the first call already destroyed all CUDA graphs and reset execution count.

2. **Timing is correct but effect is destructive**: The calls happen before the decode loop (correct timing — after shapes are frozen, before execution). But the invalidation destroys the entire execution pipeline that was built during prefill.

3. **`argTableStable_` reset**: Set to `false` by `invalidateForRebuild`. This means the fast replay path that skips refresh+ext input sync is disabled until arg table stability is re-detected (after ~2-3 warmup executions with matching address keys).

4. **`planPhase_` is NOT demoted**: `markExternalInputVariable` doesn't touch `planPhase_`. It remains at `REPLAYING`. But `executeCount_` is 0. The `executeSteadyState` precondition catches this (`executeCount_ < 3`), but the full `execute()` then sees `isFirstFrozenWarmup = true` and enters warmup. After warmup completes, the plan has to re-capture graphs, re-compile, and re-stabilize — which takes 3+ executions.
