# Task Result: claude

Subagent 'claude' completed in 111.2s

---

The test method `testChangingInputsMemoryLeak` has been added. It:

1. **Loads SmolDocling** via `ensureModelsLoaded()` (reuses existing setup)
2. **Enables DSP** with `setDspAutoCompileEnabled(true)` + `setDspNativeAutoCompileEnabled(true)`
3. **PADDED mode** (Phase 1): Runs the real decode pipeline twice via `StaticKvCacheDecodeLoop.builder().build().decode()` — first with the original prompt, then with a different prompt (`"Describe this image."`)
4. **NON-PADDED mode** (Phase 2): Sets `nd4j.dsp.noPadded=true` system property, then runs the same two decodes — attention mask shapes change every step in this mode
5. **Measures GPU memory** via `getDeviceFreeMemoryDefault()` at each phase with `[CHANGING_INPUTS]` log format
6. **Compares** padded vs non-padded total consumption in a summary table
7. **Reports per-step leak rates** for both modes to answer whether fixed shapes avoid the ~256MB/step leak

The test properly restores the `nd4j.dsp.noPadded` property in a `finally` block and cleans up allocated arrays.
