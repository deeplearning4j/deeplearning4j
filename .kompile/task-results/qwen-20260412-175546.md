# Task Result: qwen

Subagent 'qwen' completed in 198.2s

---

The test file is created. Here's a summary:

**Created:** `platform-tests/src/test/java/org/eclipse/deeplearning4j/nd4j/autodiff/samediff/TestAttentionDspVsStandard.java`

**Three test methods:**

1. **`testAttentionDspVsStandard`** — Full graph matching SmolDocling: input_embeds → Q/K/V matmul projections → `onnx_multi_head_attention` with explicit causal mask. Uses SmolDocling parameters (batch=1, seqLen=17, hidden=576, numHeads=9, headDim=64).

2. **`testAttentionWithExplicitCausalMask`** — Same as #1 but with `useCausalMask=false` (relies entirely on the explicit bias tensor).

3. **`testBareMhaDspVsStandard`** — Minimal: Q/K/V as direct placeholders → `onnx_multi_head_attention` with `useCausalMask=true`, no bias, no projections. Isolates the op itself.

**Key design:**
- Uses `SameDiff.compareExecutionPaths()` with `GraphExecutionMode.SLOT_BY_SLOT` and tolerance `1e-4`
- Fixed seed (`12345`) for reproducibility
- No model loading — runs in seconds
- Self-contained, no external dependencies
- Proper cleanup in `@AfterEach` (Triton cache invalidation, GC, memory purge)