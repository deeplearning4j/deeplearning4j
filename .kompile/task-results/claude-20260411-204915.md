# Task Result: claude

Subagent 'claude' completed in 233.8s

---

Now, the critical question is about 256MB/step leak. Let me check the sizes:

For seqLen=1 decode with maxKvLen (e.g., 2048):
- `attention_mask`: [1, 2049] LONG = 2049 * 8 = ~16KB
- `position_ids`: [1, 1] LONG = 8 bytes
- `_causal_mask`: [1, 1, 1, 2049] FLOAT = 2049 * 4 = ~8KB
- `attn_mask_reformat/Tile/output_0`: [1, 1, 1, 2049] FLOAT = ~8KB
- `input_ids`: [1, 1] LONG = 8 bytes
- `inputs_embeds`: [1, 1, hidden] FLOAT16 = hidden * 2 = ~1.2KB (hidden=576)
- `past_key_values.N.key/value`: [1, 9, maxKvLen, 64] FLOAT16 × 2 × 30 layers = 30 * 2 * 9 * 2048 * 64 * 2 = ~141MB total (but these are reused, not re-created)

None of these should cause a 256MB/step leak via dtype casting since there are **no dtype mismatches** for SmolDocling.

Here's my analysis:

## Dtype Mismatch Analysis for SmolDocling Decoder

### Result: NO dtype mismatches in `associateInternalModelInputs` or `castPlaceholderTypes`

For SmolDocling's ONNX model, every Java-created array's dtype matches the model variable's declared dtype:

| Input | Java Array Dtype | ONNX Variable Dtype | Mismatch? |
|-------|-----------------|-------------------|-----------|
| `input_ids` | LONG (INT64) | INT64 | No |
| `attention_mask` | LONG (INT64) | INT64 | No |
| `position_ids` | LONG (INT64) | INT64 | No |
| `_causal_mask` | FLOAT | FLOAT | No |
| `past_key_values.*.key/value` | FLOAT16 | FLOAT16 | No |
| `inputs_embeds` | FLOAT16 (from encoder) | FLOAT16 | No |
| `attn_mask_reformat/Tile/output_0` | FLOAT | FLOAT (Tile output) | No |

### Why `associateInternalModelInputs` probably does nothing for SmolDocling

All standard ONNX model inputs (`input_ids`, `attention_mask`, `position_ids`, `_causal_mask`, `past_key_values.*`, `inputs_embeds`) are **PLACEHOLDERs** — they appear in `externalInputs()`. The check at line 928 (`!externalInputNames.contains(inputName)`) skips them all. These go through the feed map to `output()` instead.

The `attn_mask_reformat/Tile/output_0` is converted to a PLACEHOLDER via `addPlaceholderOverride()` in `FrozenDecodeStep`, so it's also in `externalInputs()` and skipped by `associateInternalModelInputs`.

`addConfiguredInputIfInternal` (line 899) adds all configured input names unconditionally, but the external check at line 928 gates the actual association. So the method iterates but associates nothing.

### Casting paths that DO execute (but don't leak)

Both `castPlaceholderTypes` (DSP path, line 874) and `preprocessPlaceholders` (non-DSP path, line 3841) check for mismatches. Since all dtypes match, **no casts occur**.

### The 256MB/step leak is NOT from dtype casting

The per-step input sizes for seqLen=1 are tiny (total ~32KB excluding KV buffers which are reused). Even if every input were cast, that's ~32KB/step — 8000x too small to explain 256MB/step. The leak source is elsewhere.
