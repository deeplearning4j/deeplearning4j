# Task Result: qwen

Subagent 'qwen' completed in 300.5s

---

Now I have all the information I need. Let me compile my findings.

## Findings: DSP Plan Stuck at SHAPES_FROZEN

### 1. `advancePlanPhase()` conditions (NativeDynamicShapePlan.cpp:1573-1622)

The transition **SHAPES_FROZEN → POINTERS_STABLE** requires:
- `shapesFrozen_` is true (line 1583)
- `planPhase_ >= SHAPES_FROZEN` (line 1583)
- **`frozenExecutionCount_ >= 2`** (line 1586)
- **Every segment** must return `true` from `segmentHasStablePointersForPlanPhase()` (line 1589)

The transition **POINTERS_STABLE → REPLAYING** requires:
- Every replay-eligible segment must return `true` from `segmentIsFullyReplayingForPlanPhase()` (line 1612)

### 2. What makes `segmentHasStablePointersForPlanPhase` return false? (lines 137-168)

For each backend type:

- **EMULATED_REPLAY** (line 143): Returns `seg.exec.argTableStable` — this is the most likely culprit for decode loops using emulated replay
- **GPU_COMPILER** (lines 150-161): Returns true only if:
  - `currentPhase == REPLAYING` AND (`replayHandle == nullptr` OR (`replayHandle->isReady()` AND (`!needsReplayInvariantTracking` OR `argTableStable`)))
  - OR: `replayHandle->isReady()` AND (`!needsReplayInvariantTracking` OR `argTableStable`)
  - OR: `segmentIsCompiledSteadyState(seg, 1)` AND (`!needsReplayInvariantTracking` OR `argTableStable`)
- **CUDA_GRAPHS / SLOT_BY_SLOT** (line 164): Returns `seg.exec.replayHandle && seg.exec.replayHandle->isReady()`

**Key insight**: For EMULATED_REPLAY segments, `argTableStable` is the ONLY gate. If `argTableStable` is false, the segment blocks phase advancement forever.

### 3. Baseline key fix in `phaseWarmup` (lines 1757-1770) — **PRESENT and correct**

The fix IS present at lines 1763-1770:
```cpp
if (segment.def.selectedBackend == SelectedBackend::EMULATED_REPLAY) {
  segment.exec.cachedShapeKey =
      computeSegmentShapeKey(segment, externalInputs, numExternalInputs);
  segment.exec.capturedInputAddrKey =
      computeSegmentInputAddrKeyPortable(segment, externalInputs, numExternalInputs);
}
```

This correctly populates baseline shape and address keys during warmup. **However, this only covers EMULATED_REPLAY segments.** GPU_COMPILER segments do NOT get baseline keys populated here (line 1772-1774 intentional skip comment).

### 4. **`frozenExecutionCount_` increment timing — POTENTIAL ISSUE** (line 1358)

`frozenExecutionCount_` is incremented at line 1404, which is near the **end** of `execute()`:
```cpp
if (shapesFrozen_) executeCount_++;
```
Then at line 1404:
```cpp
frozenExecutionCount_++;
```

But critically, `advancePlanPhase()` is called at line 1418, **after** `frozenExecutionCount_++`. So on the first frozen execution, `frozenExecutionCount_` becomes 1, `advancePlanPhase()` runs but the `>= 2` check at line 1586 fails. On the second frozen execution, `frozenExecutionCount_` becomes 2, and `advancePlanPhase()` can proceed.

**This is correct** — the plan needs 2 frozen executions before advancing. The question is whether the decode loop actually runs 2+ frozen executions.

### 5. **DECODE LOOP CALLS `setShapesFrozen(true)` BEFORE first frozen execution — CONFIRMED**

In `StaticKvCacheDecodeLoop.java:685`:
```java
dspExec.setShapesFrozen(true);
```
This is called AFTER recompilation but BEFORE the first frozen decode step execution. This is the correct ordering.

In `FrozenDecodeStep.java:297`:
```java
dspExec.setShapesFrozen(true);
```
Same pattern — called after compile, before execute.

### 6. **ROOT CAUSE: `argTableStable` is NEVER set to true for GPU_COMPILER segments during frozen replay**

The `argTableStable` flag for **GPU_COMPILER** segments is managed in `NativeDynamicShapePlan_gpubackend.cpp`. Looking at lines 2580-2622:

- `argTableStable` is set to `true` only when `extAddrsStable && createValuesStable && shapeKeyStable` (line 2605-2608)
- But crucially, `argTableStable` is **reset to `false`** at many points during GPU_COMPILER execution:
  - Line 2444: `seg.exec.argTableStable = false;` (when compilation fails)
  - Line 2622: `seg.exec.argTableStable = false;` (when create value key mismatches)
  - Line 2656: `seg.exec.argTableStable = false;` (when Triton graph replay conditions fail)
  - Line 2947, 3159, 3237, 3454, 3479, 4609: Various failure paths

**The critical problem**: For GPU_COMPILER segments, `segmentHasStablePointersForPlanPhase` at line 150-161 checks `argTableStable` when `needsReplayInvariantTracking` is true (which is the case when `segmentHasInternalValueShapeInputs` returns true). The `needsReplayInvariantTracking` check means that segments with internal value-shape inputs (e.g., reshape, gather, broadcast_to) **require** `argTableStable` to be true.

### 7. **Most likely cause of the stall: `segmentHasInternalValueShapeInputs` returns true for decode segments**

If the decode graph contains ops like `reshape`, `gather`, `broadcast_to` that produce their output shapes from input **values** (not just input shapes), then `needsReplayInvariantTracking = true` at line 139. This means `segmentHasStablePointersForPlanPhase` will check `argTableStable`, and if `argTableStable` is false (which it starts as), the segment blocks phase advancement.

**For GPU_COMPILER segments specifically**, `argTableStable` is only set to true after a successful Triton CUDA graph capture AND when all keys match on a subsequent execution. But the first frozen execution after warmup goes through `executeSegmentWithGpuGraph` which sets `argTableStable = false` during the capture phase (line 2360, 2444) and only potentially sets it true during the **second** frozen replay execution.

### Summary of Findings

| # | File:Line | Finding |
|---|-----------|---------|
| 1 | `NativeDynamicShapePlan.cpp:1586` | `advancePlanPhase()` requires `frozenExecutionCount_ >= 2` — needs 2 full frozen executions before advancing |
| 2 | `NativeDynamicShapePlan.cpp:143` | EMULATED_REPLAY segments block advancement if `argTableStable == false` |
| 3 | `NativeDynamicShapePlan.cpp:150-161` | GPU_COMPILER segments block advancement if `needsReplayInvariantTracking && !argTableStable` |
| 4 | `NativeDynamicShapePlan.cpp:1763-1770` | Baseline key fix IS present for EMULATED_REPLAY — correctly sets `cachedShapeKey` and `capturedInputAddrKey` during warmup |
| 5 | `NativeDynamicShapePlan.cpp:1358` + `:1404` | `frozenExecutionCount_` is incremented at end of `execute()` — correct timing |
| 6 | `NativeDynamicShapePlan_gpubackend.cpp:2605-2608` | `argTableStable` is set true only when extAddrs, createValues, AND shapeKeys all match |
| 7 | `NativeDynamicShapePlan_gpubackend.cpp:2444,2622,2656` | `argTableStable` is reset to false on many error/invalidation paths |
| 8 | `NativeDynamicShapePlan_segments.cpp:1577` | `argTableStable = true` is only set when BOTH `shapeStable && addrStable` — requires 2 consecutive matching keys |

**Most likely root cause**: The decode segments contain GPU_COMPILER segments with `needsReplayInvariantTracking == true` (internal value-shape inputs). These segments require `argTableStable == true` for `segmentHasStablePointersForPlanPhase` to return true. But `argTableStable` starts as `false` and is only set to `true` after 2+ consecutive frozen executions with matching keys. If **any** key changes between frozen executions (e.g., KV cache position changes input addresses), `argTableStable` stays false, the plan never advances past SHAPES_FROZEN, and CUDA graph replay is never activated — resulting in 6.69 tok/s instead of ~90.