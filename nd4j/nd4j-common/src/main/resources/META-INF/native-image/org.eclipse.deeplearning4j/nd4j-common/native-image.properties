# Native Image configuration for ND4J
# ND4JClassLoading captures the context classloader in its static initializer.
# Build-time initialization would bake the image builder's classloader into the
# image and silently break Nd4jBackend/ServiceLoader discovery at run time.
Args = --initialize-at-run-time=org.nd4j.common.config.ND4JClassLoading
