// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.eclipse.deeplearning4j.tokenizers.bindings;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

public class TokenizersNative extends org.eclipse.deeplearning4j.tokenizers.presets.TokenizersHelper {
    static { Loader.load(); }

// Parsed from tokenizers_c.h

// #pragma once

// #include <stdarg.h>
// #include <stdbool.h>
// #include <stdint.h>
// #include <stdlib.h>

// #ifdef __cplusplus
// #endif

/**
 * \brief Opaque handle for tokenizer instance
 */
@Opaque public static class OpaqueTokenizer extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public OpaqueTokenizer() { super((Pointer)null); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public OpaqueTokenizer(Pointer p) { super(p); }
}

/**
 * \brief Opaque handle for encoding result
 */
@Opaque public static class OpaqueEncoding extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public OpaqueEncoding() { super((Pointer)null); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public OpaqueEncoding(Pointer p) { super(p); }
}

/**
 * \brief Opaque handle for a stateful incremental decoder
 */
@Opaque public static class OpaqueDecodeStream extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public OpaqueDecodeStream() { super((Pointer)null); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public OpaqueDecodeStream(Pointer p) { super(p); }
}

/**
 * \brief Opaque handle for model manager
 */
@Opaque public static class OpaqueModelManager extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public OpaqueModelManager() { super((Pointer)null); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public OpaqueModelManager(Pointer p) { super(p); }
}

/**
 * \brief Error codes for tokenizer operations
 */
/** enum TokenizerError */
public static final int
    TOKENIZER_SUCCESS = 0,
    TOKENIZER_ERROR_INVALID_INPUT = 1,
    TOKENIZER_ERROR_FILE_NOT_FOUND = 2,
    TOKENIZER_ERROR_INVALID_JSON = 3,
    TOKENIZER_ERROR_MEMORY_ALLOCATION = 4,
    TOKENIZER_ERROR_UNKNOWN = 5;

/**
 * \brief Result structure for operations that can fail
 */
public static class TokenizerResult extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public TokenizerResult() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public TokenizerResult(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TokenizerResult(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public TokenizerResult position(long position) {
        return (TokenizerResult)super.position(position);
    }
    @Override public TokenizerResult getPointer(long i) {
        return new TokenizerResult((Pointer)this).offsetAddress(i);
    }

    public native @Cast("TokenizerError") int error_code(); public native TokenizerResult error_code(int setter);
    public native @Cast("char*") BytePointer error_message(); public native TokenizerResult error_message(BytePointer setter);
}

// =============================================================================
// Core Tokenizer Functions
// =============================================================================

/**
 * \brief Create a tokenizer from a file path
 * @param model_path Path to the tokenizer configuration file
 * @return Pointer to tokenizer instance, or NULL on failure
 */
public native @Name("create_tokenizer_from_file") OpaqueTokenizer createTokenizerFromFile(@Cast("char*") BytePointer model_path);
public native @Name("create_tokenizer_from_file") OpaqueTokenizer createTokenizerFromFile(@Cast("char*") String model_path);

/**
 * \brief Create a tokenizer from JSON string
 * @param json_config JSON configuration string
 * @return Pointer to tokenizer instance, or NULL on failure
 */
public native @Name("create_tokenizer_from_json") OpaqueTokenizer createTokenizerFromJson(@Cast("char*") BytePointer json_config);
public native @Name("create_tokenizer_from_json") OpaqueTokenizer createTokenizerFromJson(@Cast("char*") String json_config);

/**
 * \brief Free a tokenizer instance
 * @param tokenizer Tokenizer instance to free
 */
public native @Name("free_tokenizer") void freeTokenizer(OpaqueTokenizer tokenizer);

/**
 * \brief Check if a tokenizer is valid
 * @param tokenizer Tokenizer instance
 * @return true if valid, false otherwise
 */
public native @Cast("bool") @Name("tokenizer_is_valid") boolean tokenizerIsValid(OpaqueTokenizer tokenizer);

/**
 * \brief Get vocabulary size
 * @param tokenizer Tokenizer instance
 * @return Vocabulary size, or 0 if invalid
 */
public native @Cast("size_t") @Name("get_vocab_size") long getVocabSize(OpaqueTokenizer tokenizer);

/**
 * \brief Resolve an exact vocabulary token without encoding it
 * @param tokenizer Tokenizer instance
 * @param token Exact token string
 * @param token_id Receives the token ID when found
 * @return true when the token exists, false when it does not or input is invalid
 */
public native @Cast("bool") @Name("get_token_id") boolean getTokenId(OpaqueTokenizer tokenizer, @Cast("char*") BytePointer token, @Cast("uint32_t*") IntPointer token_id);
public native @Cast("bool") @Name("get_token_id") boolean getTokenId(OpaqueTokenizer tokenizer, @Cast("char*") String token, @Cast("uint32_t*") IntBuffer token_id);
public native @Cast("bool") @Name("get_token_id") boolean getTokenId(OpaqueTokenizer tokenizer, @Cast("char*") BytePointer token, @Cast("uint32_t*") int[] token_id);
public native @Cast("bool") @Name("get_token_id") boolean getTokenId(OpaqueTokenizer tokenizer, @Cast("char*") String token, @Cast("uint32_t*") IntPointer token_id);
public native @Cast("bool") @Name("get_token_id") boolean getTokenId(OpaqueTokenizer tokenizer, @Cast("char*") BytePointer token, @Cast("uint32_t*") IntBuffer token_id);
public native @Cast("bool") @Name("get_token_id") boolean getTokenId(OpaqueTokenizer tokenizer, @Cast("char*") String token, @Cast("uint32_t*") int[] token_id);

/**
 * \brief Render a Hugging Face tokenizer_config.json chat template
 * @param messages_json JSON array of role/content message objects
 * @param tokenizer_config_json Full normalized tokenizer_config.json
 * @param current_date Current date exposed as date_string
 * @param add_generation_prompt Whether to append the assistant prompt
 * @return Rendered prompt, or NULL on failure (must be freed with free_string)
 */
public native @Name("apply_chat_template") @Cast("char*") BytePointer applyChatTemplate(@Cast("char*") BytePointer messages_json,
                          @Cast("char*") BytePointer tokenizer_config_json,
                          @Cast("char*") BytePointer current_date,
                          @Cast("bool") boolean add_generation_prompt);
public native @Name("apply_chat_template") @Cast("char*") String applyChatTemplate(@Cast("char*") String messages_json,
                          @Cast("char*") String tokenizer_config_json,
                          @Cast("char*") String current_date,
                          @Cast("bool") boolean add_generation_prompt);

/**
 * \brief Render a chat template from a complete Hugging Face runtime context
 * @param context_json JSON object containing messages, tools, documents, flags,
 *                     and optional model-specific template arguments
 * @param tokenizer_config_json Full normalized tokenizer_config.json
 * @param current_date Current date exposed as date_string when not overridden
 * @return Rendered prompt, or NULL on failure (must be freed with free_string)
 */
public native @Name("apply_chat_template_context") @Cast("char*") BytePointer applyChatTemplateContext(@Cast("char*") BytePointer context_json,
                                  @Cast("char*") BytePointer tokenizer_config_json,
                                  @Cast("char*") BytePointer current_date);
public native @Name("apply_chat_template_context") @Cast("char*") String applyChatTemplateContext(@Cast("char*") String context_json,
                                  @Cast("char*") String tokenizer_config_json,
                                  @Cast("char*") String current_date);

// =============================================================================
// Encoding Functions
// =============================================================================

/**
 * \brief Encode text into tokens
 * @param tokenizer Tokenizer instance
 * @param text Input text to encode
 * @param add_special_tokens Whether to add special tokens
 * @return Encoding result, or NULL on failure
 */
public native @Name("encode_text") OpaqueEncoding encodeText(OpaqueTokenizer tokenizer, @Cast("char*") BytePointer text, @Cast("bool") boolean add_special_tokens);
public native @Name("encode_text") OpaqueEncoding encodeText(OpaqueTokenizer tokenizer, @Cast("char*") String text, @Cast("bool") boolean add_special_tokens);

/**
 * \brief Encode multiple texts in batch
 * @param tokenizer Tokenizer instance
 * @param texts Array of input texts
 * @param num_texts Number of texts in array
 * @param add_special_tokens Whether to add special tokens
 * @return Array of encoding results, or NULL on failure
 */


/**
 * \brief Free an encoding result
 * @param encoding Encoding to free
 */
public native @Name("free_encoding") void freeEncoding(OpaqueEncoding encoding);

/**
 * \brief Free a batch of encoding results
 * @param encodings Array of encodings to free
 * @param num_encodings Number of encodings in array
 */
public native @Name("free_encoding_batch") void freeEncodingBatch(@Cast("OpaqueEncoding**") PointerPointer encodings, @Cast("size_t") long num_encodings);
public native @Name("free_encoding_batch") void freeEncodingBatch(@ByPtrPtr OpaqueEncoding encodings, @Cast("size_t") long num_encodings);

// =============================================================================
// Encoding Result Access Functions
// =============================================================================

/**
 * \brief Get the number of tokens in an encoding
 * @param encoding Encoding result
 * @return Number of tokens, or 0 if invalid
 */
public native @Cast("size_t") @Name("encoding_get_length") long encodingGetLength(OpaqueEncoding encoding);

/**
 * \brief Get token IDs from an encoding
 * @param encoding Encoding result
 * @return Array of token IDs, or NULL if invalid
 */
public native @Cast("const uint32_t*") @Name("encoding_get_ids") IntPointer encodingGetIds(OpaqueEncoding encoding);

/**
 * \brief Get token strings from an encoding
 * @param encoding Encoding result
 * @return Array of token strings, or NULL if invalid
 */
public native @Cast("const char**") @Name("encoding_get_tokens") PointerPointer encodingGetTokens(OpaqueEncoding encoding);

/**
 * \brief Get token offsets from an encoding
 * @param encoding Encoding result
 * @param start_offsets Output array for start offsets
 * @param end_offsets Output array for end offsets
 * @return Number of offsets, or 0 if invalid
 */
public native @Cast("size_t") @Name("encoding_get_offsets") long encodingGetOffsets(OpaqueEncoding encoding, @Cast("size_t**") PointerPointer start_offsets, @Cast("size_t**") PointerPointer end_offsets);
public native @Cast("size_t") @Name("encoding_get_offsets") long encodingGetOffsets(OpaqueEncoding encoding, @Cast("size_t**") @ByPtrPtr SizeTPointer start_offsets, @Cast("size_t**") @ByPtrPtr SizeTPointer end_offsets);

// =============================================================================
// Decoding Functions
// =============================================================================

/**
 * \brief Decode token IDs back to text
 * @param tokenizer Tokenizer instance
 * @param ids Array of token IDs
 * @param num_ids Number of IDs in array
 * @param skip_special_tokens Whether to skip special tokens
 * @return Decoded text string, or NULL on failure (must be freed with free_string)
 */
public native @Name("decode_ids") @Cast("char*") BytePointer decodeIds(OpaqueTokenizer tokenizer, @Cast("const uint32_t*") IntPointer ids, @Cast("size_t") long num_ids, @Cast("bool") boolean skip_special_tokens);
public native @Name("decode_ids") @Cast("char*") String decodeIds(OpaqueTokenizer tokenizer, @Cast("const uint32_t*") IntBuffer ids, @Cast("size_t") long num_ids, @Cast("bool") boolean skip_special_tokens);
public native @Name("decode_ids") @Cast("char*") BytePointer decodeIds(OpaqueTokenizer tokenizer, @Cast("const uint32_t*") int[] ids, @Cast("size_t") long num_ids, @Cast("bool") boolean skip_special_tokens);

/**
 * \brief Create a stateful decoder using the tokenizer's native DecodeStream
 * @param tokenizer Tokenizer instance
 * @param skip_special_tokens Whether to omit special tokens
 * @return Decoder instance, or NULL on failure
 *
 * The decoder retains the underlying Rust tokenizer and may outlive
 * {@code tokenizer}.
 */
public native @Name("create_decode_stream") OpaqueDecodeStream createDecodeStream(OpaqueTokenizer tokenizer, @Cast("bool") boolean skip_special_tokens);

/**
 * \brief Decode one token and return the next complete text chunk
 * @param stream Decoder instance
 * @param token_id Unsigned 32-bit token ID
 * @return UTF-8 chunk valid until the next call on this stream, or NULL on
 * failure. An empty string means that more tokens are required before text can
 * be emitted.
 */
public native @Name("decode_stream_step") @Cast("char*") BytePointer decodeStreamStep(OpaqueDecodeStream stream, @Cast("uint32_t") int token_id);

/**
 * \brief Free a stateful decoder
 */
public native @Name("free_decode_stream") void freeDecodeStream(OpaqueDecodeStream stream);

/**
 * \brief Free a string returned by decode operations
 * @param str String to free
 */
public native @Name("free_string") void freeString(@Cast("char*") BytePointer str);
public native @Name("free_string") void freeString(@Cast("char*") String str);

// =============================================================================
// Model Manager Functions
// =============================================================================

/**
 * \brief Create a model manager instance
 * @return Pointer to model manager, or NULL on failure
 */
public native @Name("create_model_manager") OpaqueModelManager createModelManager();

/**
 * \brief Free a model manager instance
 * @param manager Model manager to free
 */
public native @Name("free_model_manager") void freeModelManager(OpaqueModelManager manager);

/**
 * \brief Check if a model file is valid
 * @param model_path Path to model file
 * @return true if valid, false otherwise
 */
public native @Cast("bool") @Name("is_valid_model_file") boolean isValidModelFile(@Cast("char*") BytePointer model_path);
public native @Cast("bool") @Name("is_valid_model_file") boolean isValidModelFile(@Cast("char*") String model_path);

/**
 * \brief Get list of available embedded models
 * @param num_models Output parameter for number of models
 * @return Array of model names, or NULL if none available
 */
public native @Cast("const char**") @Name("get_embedded_models") PointerPointer getEmbeddedModels(@Cast("size_t*") SizeTPointer num_models);

/**
 * \brief Free embedded models list
 * @param models Array returned by get_embedded_models
 * @param num_models Number of models in array
 */


// =============================================================================
// Error Handling Functions
// =============================================================================

/**
 * \brief Get the last error that occurred
 * @return Error result structure
 */
public native @ByVal @Name("get_last_error") TokenizerResult getLastError();

/**
 * \brief Clear the last error
 */
public native @Name("clear_last_error") void clearLastError();

/**
 * \brief Get version information
 * @return Version string (do not free)
 */
public native @Name("get_tokenizer_version") @Cast("char*") BytePointer getTokenizerVersion();

/**
 * \brief Get build information
 * @return Build info string (do not free)
 */
public native @Name("get_build_info") @Cast("char*") BytePointer getBuildInfo();

// #ifdef __cplusplus
// #endif


}
