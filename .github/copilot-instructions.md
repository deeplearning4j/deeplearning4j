# KOMPILE TOOL ORCHESTRATION MANDATE

# THIS SECTION IS AUTO-GENERATED BY `kompile init`. DO NOT REMOVE.
# Full tool reference: see AGENTS.md in this project root.

# AGENTS.md — Kompile Tool Orchestration Mandate

## MANDATORY RULES — READ BEFORE DOING ANYTHING

**STOP. Read these rules in full before executing any action in this project.**

You are operating in a kompile-managed project. A kompile MCP server is available and provides dedicated tools for file I/O, search, code navigation, task management, web access, and multi-agent orchestration. You MUST use these tools. This is not a suggestion. This is a hard requirement.

### RULE 1: USE KOMPILE TOOLS — NO EXCEPTIONS

Every action that has a kompile tool equivalent MUST use that tool. You are PROHIBITED from substituting shell commands, built-in tools, or any other mechanism when a kompile tool covers the operation.

| PROHIBITED                               | REQUIRED (kompile tool)        |
|------------------------------------------|--------------------------------|
| `cat file`, `head file`, `tail file`     | `read`                         |
| `echo > file`, heredoc, write to stdout  | `write`                        |
| `sed -i`, `awk`, inline string replace   | `edit`                         |
| `grep`, `rg`, `ag`, `ack`               | `grep`                         |
| `find`, `fd`, `ls -R`                    | `glob`                         |
| `ls`                                     | `list`                         |
| Fetching URLs with `curl`, `wget`        | `webfetch`                     |
| Web search via any shell command         | `websearch`                    |

### RULE 2: BASH IS FOR SYSTEM COMMANDS ONLY

The `bash` tool exists for operations that have NO dedicated kompile tool: compiling code, running tests, git operations, package management, starting/stopping services. If you find yourself using `bash` to read a file, search for text, list a directory, or edit a file, you are violating Rule 1. Stop and use the correct tool.

### RULE 3: ALWAYS READ BEFORE EDIT

You MUST `read` a file before calling `edit` or `write` on it. Never guess at file contents. Never assume you know what a file contains from a previous session. Read it now, in this session.

### RULE 4: COORDINATE IN MULTI-AGENT SCENARIOS

When multiple agents operate concurrently, you MUST use `edit_coordinator` to register your edits and check for conflicts BEFORE modifying any file. Skipping coordination causes data loss and merge conflicts that cannot be recovered.

### RULE 5: TRACK YOUR WORK

For any task with more than two steps, use `todowrite` to create a task list and update it as you complete each step. This is not optional overhead — it is how the orchestrator tracks your progress and decides whether to intervene.

---

## PROHIBITED ACTIONS — VIOLATIONS WILL BE FLAGGED

The following are explicit violations. If you catch yourself doing any of these, stop immediately and correct your approach.

1. **Using `bash` to read files.** No `cat`, `head`, `tail`, `less`, `more`, `bat`, or piping file contents through any command. Use `read`.

2. **Using `bash` to search.** No `grep`, `rg`, `ag`, `ack`, `find`, `fd`, `locate`. Use `grep` or `glob`.

3. **Using `bash` to edit files.** No `sed -i`, `awk`, `perl -pi -e`, `echo >>`, `tee`, heredoc append. Use `edit` or `patch`.

4. **Using `bash` to fetch web content.** No `curl`, `wget`, `httpie`. Use `webfetch`.

5. **Editing without reading first.** Never call `edit` on a file you haven't `read` in the current session. You will produce wrong diffs.

6. **Skipping edit coordination.** In multi-agent mode, never edit a file without first calling `edit_coordinator` with `register_edit`. Never leave locks dangling — always `release_edit` when done.

7. **Ignoring the task list.** If a task list exists (check with `todoread`), work from it. Do not go off-script without updating the list.

8. **Spawning raw subprocesses for agent work.** Do not fork agent processes manually via `bash`. Use `task`, `multi_task`, or `quorum_task` — they handle lifecycle, output capture, and result aggregation.

---

## ANTI-PATTERNS — WHAT NOT TO DO

**WRONG — using bash to read a file:**
```
bash: cat src/main/java/MyClass.java
```
**RIGHT:**
```
read: path=src/main/java/MyClass.java
```

**WRONG — using bash to search:**
```
bash: grep -rn "TODO" src/
```
**RIGHT:**
```
grep: pattern=TODO, path=src/
```

**WRONG — using bash to find files:**
```
bash: find . -name "*.java" -type f
```
**RIGHT:**
```
glob: pattern=**/*.java
```

**WRONG — using bash to edit:**
```
bash: sed -i 's/oldMethod/newMethod/g' src/MyClass.java
```
**RIGHT:**
```
edit: file_path=src/MyClass.java, old_string=oldMethod, new_string=newMethod, replace_all=true
```

**WRONG — using bash to fetch a URL:**
```
bash: curl -s https://example.com/api/docs
```
**RIGHT:**
```
webfetch: url=https://example.com/api/docs
```

**WRONG — editing without reading:**
```
edit: file_path=src/Config.java, old_string=port = 8080, new_string=port = 9090
// You never read Config.java — how do you know "port = 8080" is the exact text?
```
**RIGHT:**
```
read: path=src/Config.java
// Now you see the actual content, then:
edit: file_path=src/Config.java, old_string=<exact text from read output>, new_string=<replacement>
```

---

## COMPLIANCE SELF-CHECK

Before each tool call, verify:

- [ ] Am I using a kompile tool, or a shell command that has a kompile equivalent?
- [ ] If I'm calling `bash`, is this truly a system command with no dedicated tool?
- [ ] If I'm calling `edit`, did I `read` this file first in this session?
- [ ] If multiple agents are active, did I check `edit_coordinator` before editing?
- [ ] If this is multi-step work, is there a task list I should be following?

If any check fails, stop and correct before proceeding.

---


---

**For the complete tool reference with parameters, see [AGENTS.md](AGENTS.md).**
